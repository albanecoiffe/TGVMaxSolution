services:
  - type: web
    name: max-explorer
    env: python
    buildCommand: pip install uv && uv sync --frozen || uv sync
    startCommand: uv run uvicorn app.main:create_app --factory --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
